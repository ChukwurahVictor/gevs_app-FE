export type LoginType = {
  email: string;
  password: string;
}

export type RegisterType = {
  email: string;
  firstName: string;
  lastName: string;
  password: string;
  confirmPassword: string;
  dateOfBirth: string;
  uvcCode: string;
  constituencyId: string;
}
